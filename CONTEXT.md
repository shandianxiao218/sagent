# sagent 领域上下文

`sagent` 是一个基于 pi 的 A 股主线交易分析 agent。它只做研究和辅助分析，不自动下单，不构成投资建议。

## 核心目标

每日收盘后扫描 A 股机会，寻找：

1. 当前市场主线板块中的强势个股。
2. 个股处于长期上升趋势或主升浪中。
3. 第一波上涨明显有力，随后出现健康回调或整理。
4. 正在突破短期回调趋势，具备可定义的买点和止损点。

## 运行方式

- 主要入口：pi project-local extension 的 `/scan` 命令。
- 触发方式：用户手动触发；后续可由 Windows Task Scheduler 在收盘后自动触发。
- 通知方式：优先飞书机器人推送；未配置 webhook 时安全跳过。
- 判断模型：默认明确使用 **GLM5.1**；配置允许时可切换 **GPT-5.5**。

## 每日 `/scan` 工作流

### 阶段一：持仓监控

1. 读取本地 `portfolio.json`。
2. 代码计算每只持仓：
   - 当前盈亏比。
   - 是否触发硬性止损：跌破买点前关键低点，或单笔亏损达到 5%。
   - 是否达到 2.5–3 盈亏比止盈半仓区间。
3. 对每只持仓生成近 5 日走势描述。
4. LLM 判断趋势状态，给出持有 / 减仓 / 清仓建议。
5. 推送结果给用户确认。

### 阶段二：新机会扫描

1. 粗筛（Python + AKShare）：
   - 全 A 股。
   - 剔除 ST、停牌、次新、日均成交额低于 1 亿的股票。
   - 当前价格在 250 日均线之上。
   - 60 日内涨幅 ≥ 50%。
   - 从阶段高点回撤幅度为前一波涨幅的 15%–50%。
   - 近 3 日开始回升。
2. 主线验证（代码 + LLM）：
   - 使用申万二级行业。
   - 回看 30 日。
   - 强主线条件：连续 5 日成交额前 10，且 30 日内至少 3 次涨幅前 10，且 30 日内至少 3 次出现 3 只以上涨停。
   - 弱主线条件：满足成交额和涨幅条件，但涨停扩散不足；由 LLM 判断是否纳入。
3. 精筛（LLM）：
   - 把候选股 K 线转成自然语言描述。
   - 判断第一波上涨是否明显有力。
   - 判断回调结构是否健康。
   - 判断是否突破短期回调趋势。
   - 识别买点前关键低点。
4. 输出买入建议：
   - 标的。
   - 建议价位。
   - 买点前关键低点止损价。
   - 预期盈亏比。
   - 主线判断。
   - 风险点。

## 风控规则

- 单次开仓金额：总资金的 10%。
- 每周最多新开仓 2 次。
- 买入后必须记录到 `portfolio.json`：
  - 股票代码、名称、行业。
  - 买入日期、买入价、数量、仓位比例。
  - 买点前关键低点。
  - 趋势破坏参考位或结构说明。
  - 已止盈数量、剩余数量。
- 止损（取较高者）：
  - 绝对止损：单笔亏损达到 10%（entry × 0.90）。
  - 关键低点止损：跌破买点前关键低点下方 3%（key_low × 0.97）。
- 止盈：
  - 盈亏比达到 2.5–3 时止盈一半。
  - 剩余仓位在趋势破坏时全部退出。

## 数据源

### 行情层（K 线 + 实时价）
- **mootdx（通达信 TCP 7709）**：K 线、五档盘口、逐笔成交 — 不封 IP，优先使用
- **腾讯财经 HTTP**：PE/PB/市值/换手率/涨跌停 — 不封 IP

### 板块/行业数据（a-stock-data）
- **东财 push2**：行业板块涨跌排名（`industry_comparison()`）— 零鉴权
- **百度股市通**：个股行业/概念/地域归属（`concept_blocks()`）— 零鉴权
- 参见：`skills/a-stock-data/SKILL.md`（V3.2）
- GitHub：https://github.com/simonlin1212/a-stock-data

### 数据源优先级（来自 a-stock-data V3.2）
1. mootdx/腾讯 — 不封 IP，优先
2. 东财 push2 — 行业板块（零鉴权）
3. 百度股市通 — 概念板块（零鉴权）
4. 东财 datacenter — 龙虎榜/解禁（需限流）

### 日线缓存（SQLite）
- 本地缓存 3 年日线到 `data/bars.db`
- 增量更新只补缺失交易日
- 向量化解析（替代 iterrows）

## 关键术语

### 主线

主线 = 当前资金、涨幅、涨停扩散同时占优的申万二级行业。策略优先选择强主线，也允许 LLM 对弱主线作二次判断。

### 买点前关键低点

买点前关键低点不是简单的前 N 日最低价，而是利弗摩尔体系中的同级别结构转折低点。LLM 在读取 K 线描述时需要识别其结构意义。

### 趋势破坏

趋势破坏指更大级别上升趋势结构被破坏，通常体现为跌破同级别上升趋势的最后一个关键低点。它和买点前关键低点止损同源，但关注级别更大、用于剩余仓位退出。

## 输出规范

任何分析建议必须包含：

- 建议动作：买入 / 观察 / 放弃 / 持有 / 减仓 / 清仓。
- 判断理由。
- 所属板块是否主线。
- 买点前关键低点。
- 无效条件和风险点。
- 使用的判断模型。
- 风险提示：仅作研究和辅助分析，不构成投资建议。

## 禁止事项

- 不自动下单。
- 不承诺收益。
- 不把 LLM 判断当作事实。
- 不省略数据延迟、数据错误、模型误判和流动性风险。
