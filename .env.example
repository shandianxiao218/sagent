# ===== 飞书推送（可选）=====
# 配置后 /scan 自动推送结果到飞书群机器人
SAGENT_FEISHU_WEBHOOK=

# ===== LLM =====
# LLM 由 pi 自身管理（pi settings.json 中的 model 配置）。
# 无需在此配置 API Key。pi agent 会使用其内置模型做板块和个股判断。
# Python 侧的规则引擎 fallback 在无 pi 时自动生效。
